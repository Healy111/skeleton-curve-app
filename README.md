# 骨架曲线提取器 Web版

基于Streamlit的骨架曲线提取器Web应用，用于从滞回曲线数据中提取骨架曲线。

## 功能特点

- 📁 文件上传：支持.txt格式数据文件
- ⚙️ 参数调整：可调整平滑因子和插值点数
- 📊 可视化：显示原始数据、骨架曲线、包络线和平滑结果
- 💾 数据导出：下载处理后的骨架曲线数据

## 数据格式要求

数据文件应为.txt格式，包含两列数据：
- 第一列：位移数据
- 第二列：力数据

列之间用空格或制表符分隔，不需要表头。

## 本地运行

1. 安装依赖：
```bash
pip install -r requirements.txt